import { PipeHoursPipe } from './pipe-hours.pipe';

describe('PipeHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
